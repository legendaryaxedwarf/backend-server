DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=apply_db

# python3 -c "import secrets; print(secrets.token_hex(32))" 로 직접 생성해서 채우기
JWT_SECRET_KEY=

# 포트폴리오 파일 업로드 저장 위치 (nginx WEB_ROOT 하위, 기본값 그대로면 생략 가능)
# PORTFOLIO_UPLOAD_DIR=/var/www/html/uploads/portfolio
# PORTFOLIO_UPLOAD_URL_PREFIX=/uploads/portfolio
